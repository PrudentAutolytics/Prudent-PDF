'use strict';
const fs=require('fs'),path=require('path'),crypto=require('crypto');
const root=path.resolve(__dirname,'..');
const read=p=>fs.readFileSync(path.join(root,p),'utf8');
const check=(ok,m)=>{if(!ok){console.error('FAIL:',m);process.exitCode=1}else console.log('PASS:',m)};
const viewer=read('pages/viewer.html');

check(viewer.includes('v38 premium secure review command bar'),'premium viewer command bar theme exists');
check(viewer.includes('Secure document review'),'viewer has a clear review identity');
check(viewer.includes('viewer-selection'),'document selection has a dedicated region');
check(viewer.includes('viewer-actions'),'export actions have a dedicated region');
check(viewer.includes('Open original'),'original action is explicit');
check(viewer.includes('Open redacted'),'redacted action is explicit');
check(viewer.includes('Export review CSV'),'review export action is explicit');
check(viewer.includes('Controlled redacted output'),'redacted pane clearly identifies the controlled result');
check(viewer.includes('Original document'),'original pane is clearly identified');
check(viewer.includes('Original and redacted outputs stay separated and integrity checked.'),'review assurance is concise and visible');
check(viewer.includes('aria-label="Secure document review controls"'),'toolbar has an accessible label');
check(viewer.includes('aria-label="Document export actions"'),'action group has an accessible label');
check(viewer.includes('title="Open the original PDF in a new tab"'),'original action has help text');
check(viewer.includes('title="Open the controlled redacted PDF in a new tab"'),'redacted action has help text');
check(viewer.includes('@media(max-width:1020px)'),'viewer has tablet command bar behavior');
check(viewer.includes('@media(max-width:760px)'),'viewer has mobile command bar behavior');
check(viewer.includes('@media(max-width:480px)'),'viewer has narrow phone behavior');
check(!viewer.includes('>Original</button>'),'old ambiguous original button label is removed');
check(!viewer.includes('>Redacted PDF</button>'),'old inconsistent redacted button label is removed');
check(!/[\u2013\u2014]/.test(viewer),'viewer source contains no en dash or em dash');

const locked=crypto.createHash('sha256').update(fs.readFileSync(path.join(root,'api/jobs-submit/index.js'))).digest('hex');
check(locked==='5345cc76c6e06ad5bc9d5cb18b50ea0f4c039a7306a2e07d1e72db6a32702803','locked Power Automate contract remains unchanged');
if(process.exitCode)process.exit(process.exitCode);
console.log('V38 Viewer command bar gate passed.');
